# 🚀 Инструкция по запуску проекта

## Предварительные требования

Перед началом работы убедитесь, что у вас установлено:

- **Node.js** (версия 18 или выше) - [скачать](https://nodejs.org/)
- **Docker Desktop** - [скачать](https://www.docker.com/products/docker-desktop/)
- **Git** - [скачать](https://git-scm.com/)

## 📦 Шаг 1: Клонирование проекта

```bash
git clone git@github.com:ishenkulovve/scraping-brands.git
cd scraping-brands
```

## 📥 Шаг 2: Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые пакеты из `package.json`.

## 🗄️ Шаг 3: Настройка базы данных PostgreSQL

### Вариант А: Автоматическая настройка (рекомендуется)

```bash
chmod +x setup-db.sh
./setup-db.sh
```

Этот скрипт:
- Запустит PostgreSQL в Docker контейнере
- Создаст файл `.env` с настройками подключения
- Запустит миграции Prisma
- Сгенерирует Prisma Client

### Вариант Б: Ручная настройка

1. **Запустите PostgreSQL через Docker:**

```bash
npm run db:start
```

2. **Создайте файл `.env` в корне проекта:**

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/scraper_db?schema=public"
```

3. **Запустите миграции:**

```bash
npm run prisma:migrate
```

4. **Сгенерируйте Prisma Client:**

```bash
npm run prisma:generate
```

## ⚙️ Шаг 4: Настройка скрапера (опционально)

Если вы хотите скрапить свой сайт, отредактируйте файл `src/index.ts`:

```typescript
const TARGET_WEBSITE = 'https://your-website.com'; // Замените на ваш URL
```

И настройте селекторы в `src/scrapers/catalogScraper.ts` под структуру вашего сайта.

## ▶️ Шаг 5: Запуск проекта

### Запустить скрапер один раз:

```bash
npm run scrape
```

### Запустить в режиме разработки (с автоперезагрузкой):

```bash
npm run dev
```

### Запустить скомпилированную версию:

```bash
npm run build
npm start
```

## 📊 Шаг 6: Просмотр данных в базе

Откройте Prisma Studio для визуального просмотра данных:

```bash
npm run prisma:studio
```

Prisma Studio откроется в браузере по адресу: `http://localhost:5555`

## 🛠️ Полезные команды

| Команда | Описание |
|---------|----------|
| `npm run scrape` | Запустить скрапер |
| `npm run dev` | Режим разработки с hot-reload |
| `npm run build` | Собрать проект |
| `npm start` | Запустить собранный проект |
| `npm run test` | Запустить тестовый скрипт |
| `npm run db:start` | Запустить PostgreSQL |
| `npm run db:stop` | Остановить PostgreSQL |
| `npm run db:logs` | Просмотреть логи PostgreSQL |
| `npm run prisma:studio` | Открыть Prisma Studio |
| `npm run prisma:generate` | Сгенерировать Prisma Client |
| `npm run prisma:migrate` | Создать/применить миграции |
| `npm run prisma:reset` | Сбросить базу данных |

## 🔍 Проверка работы

После запуска скрапера вы должны увидеть в консоли:

```
🔍 Starting to scrape: https://your-website.com
✅ Found 150 catalog links
💾 Saved 145 new links to database
📊 Scraping completed!
```

## ❓ Частые проблемы

### Проблема: "Docker is not running"

**Решение:** Запустите Docker Desktop и подождите пока он полностью загрузится.

### Проблема: "Port 5432 is already allocated"

**Решение:** Порт 5432 уже занят другим процессом PostgreSQL.

```bash
# Остановите текущий контейнер
npm run db:stop

# Или измените порт в docker-compose.yml
```

### Проблема: "Error: P1001: Can't reach database server"

**Решение:** База данных не запущена или недоступна.

```bash
# Проверьте статус контейнера
docker ps

# Перезапустите базу данных
npm run db:stop
npm run db:start
```

### Проблема: Prisma Client не найден

**Решение:** Сгенерируйте Prisma Client заново:

```bash
npm run prisma:generate
```

## 📝 Структура проекта

```
scraping-brands/
├── src/
│   ├── index.ts              # Точка входа
│   ├── scrapers/
│   │   └── catalogScraper.ts # Логика скрапинга
│   ├── lib/
│   │   └── prisma.ts         # Клиент Prisma
│   └── test-scraper.ts       # Тестовый скрипт
├── prisma/
│   └── schema.prisma         # Схема базы данных
├── docker-compose.yml        # Конфигурация Docker
├── package.json              # Зависимости проекта
└── tsconfig.json             # Конфигурация TypeScript
```

## 🎯 Следующие шаги

1. ✅ Проект запущен
2. 📝 Настройте селекторы под ваш сайт
3. 🚀 Запустите скрапер
4. 📊 Проверьте данные в Prisma Studio
5. 🔄 Настройте расписание запуска (cron, GitHub Actions)

## 🆘 Нужна помощь?

- Проверьте документацию в `README.md`
- Изучите примеры в `src/examples/`
- Посмотрите `QUICK_START.md` для быстрого старта

---

**Удачного скрапинга! 🎉**
