# 🎯 НАЧНИТЕ ЗДЕСЬ

## ✅ Проект настроен на PostgreSQL!

Все файлы готовы. Теперь вам нужно запустить PostgreSQL и применить миграции.

---

## 🚀 БЫСТРЫЙ СТАРТ (3 шага)

### Шаг 1: Запустите PostgreSQL

**Самый простой способ - Docker:**

```bash
docker-compose up -d
```

Это запустит PostgreSQL на порту 5432.

### Шаг 2: Настройте окружение

Создайте файл `.env` в корне проекта:

```bash
cat > .env << 'EOF'
DATABASE_URL="postgresql://scrapper:password@localhost:5432/scrapper_db"
TARGET_WEBSITE="https://example.com"
EOF
```

### Шаг 3: Примените миграции

```bash
# Удалите старые миграции SQLite
rm -rf prisma/migrations

# Создайте новые миграции для PostgreSQL
npx prisma migrate dev --name init

# Запустите тест
npm test
```

**Готово!** 🎉

---

## 📋 Альтернативный способ - Автоматический скрипт

Запустите один скрипт, который сделает всё за вас:

```bash
./setup-db.sh
```

Скрипт:
1. Запустит PostgreSQL в Docker
2. Создаст .env файл
3. Применит миграции
4. Запустит тесты

---

## 🔍 Проверка что всё работает

После настройки выполните:

```bash
npm test
```

Должны увидеть:
```
✅ Подключение к базе данных успешно
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
```

---

## 📊 Полезные команды

```bash
# База данных
docker-compose up -d          # Запустить PostgreSQL
docker-compose down           # Остановить PostgreSQL
docker-compose logs -f        # Показать логи

# Prisma
npx prisma studio             # Открыть GUI базы данных
npx prisma migrate dev        # Создать новую миграцию
npx prisma generate           # Обновить клиент

# Скраппер
npm run scrape                # Запустить скраппер
npm test                      # Запустить тесты
```

---

## 📚 Документация

- **MIGRATION_GUIDE.md** - Инструкция по миграции с SQLite
- **SETUP_POSTGRESQL.md** - Подробная настройка PostgreSQL
- **README.md** - Основная документация
- **QUICK_START.md** - Как использовать скраппер

---

## ❓ Нет Docker?

### Вариант 1: Установите Docker
- macOS: https://www.docker.com/products/docker-desktop
- Windows: https://www.docker.com/products/docker-desktop
- Linux: `curl -fsSL https://get.docker.com | sh`

### Вариант 2: Локальный PostgreSQL
```bash
# macOS
brew install postgresql@16
brew services start postgresql@16

# Затем создайте БД
psql postgres
CREATE USER scrapper WITH PASSWORD 'password';
CREATE DATABASE scrapper_db OWNER scrapper;
\q
```

### Вариант 3: Облачный PostgreSQL (бесплатно)
- Supabase: https://supabase.com/
- Render: https://render.com/
- Neon: https://neon.tech/

После создания скопируйте Connection String в .env

---

## 🎯 Следующие шаги

После успешной настройки:

1. ✅ PostgreSQL работает
2. ⏭️ Настройте селекторы под ваш сайт (`src/scrapers/catalogScraper.ts`)
3. ⏭️ Укажите URL сайта (`src/index.ts`)
4. ⏭️ Запустите скраппер (`npm run scrape`)
5. ⏭️ Создайте модели ProductLink и Product
6. ⏭️ Добавьте скраппер для продуктов

---

**Успехов! 🚀**

